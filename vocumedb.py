# import chromadb

# # Create a Settings object with allow_reset set to True
# settings = chromadb.Settings(allow_reset=True)

# # Create a PersistentClient with the custom settings
# client = chromadb.PersistentClient(path="chroma_db", settings=settings)

# # Now you can call the reset method
# client.reset()



